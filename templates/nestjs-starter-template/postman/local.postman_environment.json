{
  "name": "nestjs-starter — local",
  "_postman_variable_scope": "environment",
  "values": [
    { "key": "baseUrl", "value": "http://localhost:3000/api", "enabled": true },
    {
      "key": "adminEmail",
      "value": "admin@example.com",
      "enabled": true,
      "description": "Same as INITIAL_SUPER_ADMIN_EMAIL used by `npm run db:seed`"
    },
    {
      "key": "adminPassword",
      "value": "ChangeMe!12345",
      "type": "secret",
      "enabled": true,
      "description": "Current working password. Starts as INITIAL_SUPER_ADMIN_PASSWORD; auto-updated to adminNewPassword after the forced first-login change."
    },
    {
      "key": "adminNewPassword",
      "value": "E2eAdmin!12345",
      "type": "secret",
      "enabled": true,
      "description": "Used once by the change-initial-password step. Min 10 chars, upper + lower + digit + symbol."
    },
    {
      "key": "runManual",
      "value": "false",
      "enabled": true,
      "description": "Set to true to run the '99 Manual' folder"
    },
    { "key": "manualEmail", "value": "", "enabled": true },
    {
      "key": "manualToken",
      "value": "",
      "enabled": true,
      "description": "Token from the emailed link (Ethereal preview URL in server logs)"
    },
    {
      "key": "manualCode",
      "value": "",
      "enabled": true,
      "description": "Verification / OTP code from the email"
    }
  ]
}
